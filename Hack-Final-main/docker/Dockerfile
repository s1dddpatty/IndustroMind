# ── Production Dockerfile (aliased from root) ──
# This file exists so docker/ can be used as a build context if needed.
# The main Dockerfile is at the project root.
# To build: docker build -f docker/Dockerfile ..
